# 04 — Site Architecture (sitemap + page-by-page)

## Sitemap (7 primary pages)

```
/
/about
/who-we-serve            (or /audiences)
/the-unmasked-method     (NEW — the crown jewel; currently missing)
/services
/blog
/book                    (booking/contact + FAQ)
/contact                 (or fold into /book)
/legal (disclaimer)      (can be a footer modal, but must exist as a page/route)
```

Navigation (desktop): **Home · Who We Serve · The Method · Services · About · Blog · [Book a Call]** (CTA button).

---

## Home (`/`)

1. **Hero** — "Drop the Mask." + one sharp promise. Mask-reveal animation. Primary CTA "Book an intro call" + secondary "Explore The Method."
2. **The problem** — the "hidden curriculum." Name the system, not the person. (2–3 lines + a stat: 48.2% burnout.)
3. **Who we serve** — 3 audience cards (clear hierarchy: one is primary).
4. **The Unmasked Method** — teaser with a link to the full page.
5. **Social proof** — testimonial(s) + credentials strip (MA, APCC · PhD candidate · PHP/IOP director).
6. **Free toolkit** — email capture ("The Unmasked Career" lead magnet).
7. **Final CTA** — book a call.

## Who We Serve (`/who-we-serve`)

Three personas, each with **pain → what the system does wrong → how we help → which service**:
1. Pre-licensed associates (AMFT/ASW/APCC).
2. Burned-out / mid-career clinicians stepping into leadership.
3. Neurodivergent & ADHD professionals/leaders.

## The Unmasked Method (`/the-unmasked-method`)

**The missing centerpiece.** Structure (fill real steps from the client — see open questions):
- Intro: why "just try harder" fails; the interest-based nervous system.
- The framework (propose 4–5 steps, e.g. *See the System → Drop the Mask → Build Your Map → Lead Unmasked*), each with a short, plain-language explanation.
- Who it's for / outcomes.
- CTA.

> ⚠️ **Open question for client:** the exact steps/philosophy of "The Unmasked Method" — this is the IP, don't fabricate it. Ask before finalizing copy.

## Services (`/services`)

Four tiers from `02-services-pricing.md`. Clean cards with price, audience, included list. Comparison feel, not a price list. CTA per card.

## About (`/about`)

- Mission + the **six core values** (designed as an editorial list, not bullet spam).
- **The founder** at the BOTTOM (stand-alone brand): Tyler's story, credentials, the quote *"Breaking through invisible ceilings is exhausting when the system wasn't built for you…"*
- Legal disclaimer.

## Blog (`/blog`)

- The two recovered posts (see `06-blog-content.md`) + 4–6 planned new posts (titles only as placeholders).
- Cited sources (Tyler's explicit academic requirement — every post needs references).

## Book / Contact (`/book`)

- Calendly embed + contact form.
- **Prominent** legal disclaimer.
- FAQ (recover + rewrite the 6 generic FAQ answers into brand-voice).

## Footer (all pages)

Newsletter signup · LinkedIn · Instagram · legal disclaimer · © Unmasked Career Lab, LLC · 408 South Spring St, Los Angeles, CA 90013.
